@tailwind base;
@tailwind components;
@tailwind utilities;
@tailwind variants;

@layer utilities {
	.min-h-inherit {
		min-height: inherit;
	}
}

html, body {
	width: 100%;
	height: 100%;
}

.dark .big-header::after {
	background-color: rgb(var(--theme-font-color-dark)) !important;
}